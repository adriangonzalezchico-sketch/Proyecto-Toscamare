# Este archivo solo se usa cuando haces 'npm run build'
# Al dejar VITE_API_URL vacío, la web usará rutas relativas (ej: /api/contact.php)
# Esto es lo ideal para IONOS si subes la carpeta 'api' a la raíz.

VITE_API_URL=
